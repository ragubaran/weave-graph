#![deny(unsafe_code)]
//! Stub crate. Delta ingestion/publish service — not implemented until
//! the `hub` feature is scheduled (Phase 3). Never linked into the
//! default binary. Exists now so the workspace dependency graph is
//! stable from day one.
